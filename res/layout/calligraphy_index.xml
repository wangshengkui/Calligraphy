<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="horizontal" >

    <TextView
        android:id="@+id/calligraphy_index_textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_marginLeft="10dip"
        android:layout_marginTop="10dip"
        android:text="云记事目录"
        android:textSize="20dip" />

    <Button
        android:id="@+id/calligraphy_index_cancel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:text="退出目录" />

    <ListView
        android:id="@+id/calligraphy_index_listView"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_above="@id/calligraphy_index_cancel"
        android:layout_below="@id/calligraphy_index_textView"
        android:layout_margin="10dip"
        android:background="#012345" >
    </ListView>

</RelativeLayout>